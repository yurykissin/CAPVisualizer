# Run outputs (may contain sensitive tenant configuration)
output/*
!output/.gitkeep

# Secrets / local config
*.local.*
.env
*.pfx
*.cer
*.key
*.pem
secrets.json
appsettings.local.json

# Transcripts / logs
*.log
transcript-*.txt

# OS / editor cruft
.DS_Store
.vscode/
.idea/
Thumbs.db

# Generated demo artifact (regenerated by samples/Test-Offline.ps1)
samples/sample-visual.html

# Local-only name dictionary (never share; contains real tenant names)
names.json
**/raw/names.json
